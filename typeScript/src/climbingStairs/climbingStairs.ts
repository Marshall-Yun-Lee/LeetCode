export const climbingStairs = (input: number): number => {
    if (input === 0 || input === null) {
        return 0;
    } else if (input === 1) {
        return 1;
    }

    // the number itself + 1s
    // use fibonacci?
    let out = 0;
    return out;
}